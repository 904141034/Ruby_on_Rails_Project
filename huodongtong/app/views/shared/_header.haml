.navbar.navbar-default.navbar-fixed-top
  .container
    = link_to "活动通", '#', id: "logo"
    %nav
      %ul.tna.nav.pull-right
        -if current_user||current_admin
          .login_title
            你好，
            -if current_admin
              管理员
              \#{link_to "退出", logout_path, method: 'delete', class: 'logout_title'}
            -if current_user
              \#{p current_user.name}
              =link_to '退出',manager_index_path
