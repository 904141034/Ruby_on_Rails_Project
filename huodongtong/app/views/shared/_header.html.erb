<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <%= link_to "活动通", '#', id: "logo" %>
    <nav>
      <ul class=" tna nav pull-right ">
        <% if current_user %>
            <div class="login_title">你好，<%= link_to current_user.name %></div>
        <% else %>
            <%= link_to "注册", signup_path %>
            <%= link_to "登录", root_path %>
        <% end %>
      </ul>
    </nav>
  </div>
</div>