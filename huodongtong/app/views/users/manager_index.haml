.single-form-container
  .well.well-lg.panel_heading
    .manager-index-header
      用户管理 &nbsp&nbsp&nbsp
      %button
        = link_to "+添加用户"
  %table.table.panel-first-row
    %tr
      %td(align="center")  编号
      %td(align="center")  用户名
      %td(align="center")   操作
      -  id=(@us-1)*10
        - @user.each do |p|
